"use strict";

function check(form) {
    if (form.userid.value=="skanehfud279" && form.userpassword.value=="tmvkdlzm279@")
        window.open('target.html');
    else
        alert("Error UserID or Password");
}